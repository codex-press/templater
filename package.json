{
  "name": "templater",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "debug": "devtool main.js",
    "build-watch": "babel src -w -d build --ignore *.test.js",
    "run-watch": "watch 'node build/main.js' --wait=0",
   "test": "watch 'mocha --compilers js:babel-core/register src/**/*.test.js' src --wait=0",
    "start": "npm run build-watch & npm run run-watch"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "devtool": "^2.3.1",
    "snapdragon": "^0.11.0",
    "snapdragon-capture": "^0.2.0",
    "snapdragon-capture-set": "^1.0.1",
    "watch": "^1.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "chain": "^0.1.3",
    "mocha": "^3.5.0",
    "sinon": "^3.2.1"
  }
}
